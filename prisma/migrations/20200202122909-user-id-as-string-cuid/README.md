# Migration `20200202122909-user-id-as-string-cuid`

This migration has been generated by Guillaume Lamanda at 2/2/2020, 12:29:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Post" (
    "author" TEXT   ,
    "content" TEXT   ,
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" INTEGER NOT NULL  ,
    "published" BOOLEAN NOT NULL DEFAULT false ,
    "slug" TEXT NOT NULL DEFAULT '' ,
    "title" TEXT NOT NULL DEFAULT '' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("author") REFERENCES "User"("id") ON DELETE SET NULL
) 

INSERT INTO "quaint"."new_Post" ("author", "content", "createdAt", "id", "published", "slug", "title", "updatedAt") SELECT "author", "content", "createdAt", "id", "published", "slug", "title", "updatedAt" FROM "quaint"."Post"

DROP TABLE "quaint"."Post";

ALTER TABLE "quaint"."new_Post" RENAME TO "Post";

CREATE UNIQUE INDEX "quaint"."Post.slug" ON "Post"("slug")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200201192233-add-post-slug..20200202122909-user-id-as-string-cuid
--- datamodel.dml
+++ datamodel.dml
@@ -1,22 +1,25 @@
 generator client {
   provider = "prisma-client-js"
 }
+// It miss a good way to switch datasource based on environment
+// https://github.com/prisma/prisma2/blob/master/docs/prisma-schema-file.md#data-sources
+
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model User {
-  id    String  @default(cuid()) @id
+  id    String  @id @default(cuid())
   email String  @unique
   name  String?
   posts Post[]
 }
 model Post {
-  id        String   @default(cuid()) @id
+  id        Int      @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   published Boolean  @default(false)
   title     String
```


